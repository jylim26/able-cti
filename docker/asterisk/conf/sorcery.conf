; 내선(endpoint/auth/aor)은 DB가 단일 원천. pjsip.conf 폴백 없음.
; contact는 매핑하지 않고 astdb에 둔다.
; 이 파일은 모듈 로드 시 고정되므로 변경하면 컨테이너를 재시작해야 한다.

[res_pjsip]
endpoint=realtime,ps_endpoints
auth=realtime,ps_auths
aor=realtime,ps_aors
